# TODO — рабочий бэклог

Канонический поэтапный план: `ROADMAP.md`. Здесь — **конкретные следующие шаги** и отложенные инициативы; по мере выполнения пункты переносятся в `CHANGELOG_AI.md` и кратко отражаются в `CURRENT_STATE.md`.

## Ближайшие задачи

1. **Клиент Flutter:** MVP + каталог пресетов (`GET /v1/meta/personality-presets`) на экране создания. Далее: `go_router`, полноценный онбординг SRS (дельты от пресета, валидация на backend), полировка UX, CI `flutter analyze`.
2. **Проверка e2e:** вызовы OpenAPI против локального PostgreSQL (создание нейродруга, голос, сообщения, архив треда при необходимости).
3. **Память и дальше по дорожной карте:** консолидация (worker), importance/decay, дублирование чанков в Postgres при необходимости; инициатива (этап 7 в `ROADMAP.md`). Базовый Qdrant + retrieval — см. `CHANGELOG_AI.md` / этап 6 `ROADMAP.md`.

## Бэклог по этапам (кратко)

| Этап | Статус (на 2026-04-13) |
|------|-------------------------|
| 1 — Каркас, БД, health | Сделано (см. `CURRENT_STATE.md`) |
| 2 — Голос Whisper + LLM + TTS | Реализовано на backend (`/v1/perception/audio`) |
| 3 — Чат-дубликат, 500 / архив / 10 | Реализовано на backend (уточнить автотесты сценария rollover при доработках) |
| 4 — UI инспектора (события, отношения) | Базовый экран в `mobile` (события + primary relationship) |
| 5 — Онбординг, пресеты | Каталог API + JSON + выбор пресета в клиенте; дельты/валидация — в бэклоге |
| 6 — Память / семантика | Частично: Qdrant + embeddings + retrieval + индексация; консолидация/decay — впереди |
| 7–8 — Инициатива, observability | По `ROADMAP.md` |

## Отказы и отложено (с обоснованием)

| Тема | Решение | Где зафиксировано |
|------|---------|-------------------|
| Отдельный веб-стек (React/Next) только для web | Не делаем на MVP: дублирование UI и контрактов | D-001 `DECISIONS.md` |
| Микросервисы на старте | Модульный монолит FastAPI | D-002 |
| Docker Postgres как обязательный dev | Приоритет локальный PostgreSQL на хосте | D-008, `infra/README.md` |
| Чат как единственный источник истины | Отклонено: истина в событиях/памяти | D-005 |

*(Новые отказы добавлять сюда таблицей и при необходимости дублировать запись в `DECISIONS.md`.)*
