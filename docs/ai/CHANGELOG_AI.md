# История изменений (AI-память)

- **2026-04-26:** Усилен voice guard: self-echo теперь сравнивается не только с `playback_guard_text`, но и с последними assistant messages из chat history, поэтому intro/ответы нейродруга ловятся даже при рассинхроне клиента. STT hallucination guard применяется ко всему voice flow до speaker/person logic. Извлечение имени игнорирует assistant-like фразы (`нейродруг`). Flutter onboarding показывает мягкое предупреждение при возможном mismatch имени и gender-style манеры. Проверки: backend `37 passed`, `dart analyze lib` без замечаний.
- **2026-04-26:** Self-voice acoustic profile: при срабатывании `playback_guard_text` backend сохраняет WAV-профиль голоса нейродруга как системного участника `assistant_self`; последующие похожие фрагменты hands-free отбрасываются как `voice_self_profile_guard` ещё до Whisper/LLM. `assistant_self` исключён из подсчёта человеческих участников. Flutter передаёт `playback_guard_text` не только во время проигрывания, но и короткое время после него. Проверки: backend `37 passed`, `dart analyze lib` без замечаний.
- **2026-04-26:** STT hallucination guard для hands-free: перед Whisper отбрасываются WAV-чанки с низкой голосовой активностью; после Whisper типичные hallucination-фразы на тишине (`thank you for watching`, `subscribe to my channel`, `welcome back to my channel`, `share this video with your friends on social media`) логируются как `voice_stt_hallucination_guard` и не попадают в чат/LLM/TTS. Проверки: backend `36 passed`, `dart analyze lib` без замечаний.
- **2026-04-26:** Wake-by-name/barge-in MVP: `/v1/perception/audio` получил `wake_check` и `playback_guard_text`; ручная запись отвечает как раньше, hands-free режим игнорирует неадресованные фразы, а совпадение с последней озвученной репликой помечает как `assistant_self` без создания участника. Flutter больше не глушит микрофон во время TTS: можно перебить нейродруга другой фразой, а backend отсекает эхо собственной речи. `sha256_audio_mvp` заменён на `wav_acoustic_mvp` для первичного различения голосов. Проверки: backend `34 passed`, `dart analyze lib` без замечаний.
- **2026-04-26:** Voice addressing + participants MVP: добавлены `speaker_recognition_enabled`, `voice_addressing_enabled`, `ConversationParticipant` + migration `20260426_6`, `SpeakerIdentityService` с временным `sha256_audio_mvp`, первый голос становится `user_main`, новые голоса сохраняются как `guest_*`, имя сохраняется из самопредставления, voice events/chat metadata получают speaker fields, orchestrator получает speaker context, добавлен `/debug/participants`. Проверки: backend `31 passed`, `dart analyze lib` без замечаний, `alembic upgrade head` успешен.
- **2026-04-26:** v4.3 Stage B conflict/boundaries: добавлен `boundary_response_enabled`, migration `20260426_5`, conflict/boundary поля в `InternalStateSnapshot` и `RelationshipModel`, `BoundaryResponseService` с disrespect/repair detection и boundary mode, обновление affect/relationship state из входящего текста, boundary context в LLM orchestrator, anti-flattery rule, подавление инициативы при активном конфликте. Проверки: backend `28 passed`, `dart analyze lib` без замечаний, `alembic upgrade head` успешен.
- **2026-04-26:** Пункт 1 `TODO.md` (память/инициатива): добавлен `MemoryItem` + миграция `20260426_4`, SQL consolidation из `EventLog` в memory items и daily summary, importance scoring, decay/access score, internal `/v1/internal/memory/consolidate`; initiative quiet hours теперь учитывает timezone пользователя; добавлен `scripts/initiative_worker.py`; Flutter чат polling каждые 30 сек для инициативных сообщений. В `TODO.md` добавлены будущие требования wake/addressing и speaker recognition/new voice onboarding. Проверки: backend `23 passed`, `dart analyze lib` без замечаний, `alembic upgrade head` успешен.
- **2026-04-26:** Выполнены первые 3 пункта `TODO.md`: Flutter получил первую SRS-итерацию onboarding (`go_router`, welcome, preset gallery/preview, имя/голос, personalization sliders, identity lock, birth step); backend create принимает `selected_preset_id` и `personalization`, применяет deltas только в conservative preset range; добавлен `backend/scripts/full_stack_smoke.py`. Реальный `alembic upgrade head` на локальном PostgreSQL успешен; базовый HTTP smoke против `uvicorn` успешен. Проверки: backend `20 passed`, `flutter pub get`, `dart analyze lib` без замечаний.
- **2026-04-26:** Стабилизация + Stage A addendum v4.3: backend e2e-тесты create→message→history и rollover на SQLite test DB; JSONB-модели получили SQLite variant для тестов; Flutter показывает плашку обработки голосового раунда. Добавлены `BiographyProfile`, `BiographyService`, `expertise_profile_json`, `ExpertiseService`, debug endpoints biography/expertise, biography/expertise context в LLM orchestrator. Проверки: backend `19 passed`, mobile `dart analyze lib` без замечаний.
- **2026-04-26:** `TODO.md` сведен в единый бэклог: текущий SRS/ROADMAP-долг + delta-spec `NeuroFriend_Addendum_v4_3.md`. Добавлен порядок внедрения v4.3: Biography+Expertise → Conflict/Boundaries → Romantic Dynamics → Device Capabilities → Reminders, с тестовыми ожиданиями и правилами обновления документации.
- **2026-04-13:** Этап 7 (продолжение): `generate_initiative_ping`, одна исходящая реплика в чат (`append_assistant_only`), кулдаун `INITIATIVE_COOLDOWN_HOURS`, `POST /v1/internal/initiative/sweep` + `X-Initiative-Sweep-Key`; rollover треда с `incoming_messages=1`. `LOCAL_SETUP_WINDOWS.md` — curl для планировщика.
- **2026-04-13:** Этап 7 (первая итерация): `GET /v1/neurofriends/{id}/initiative/status` — gap с последнего inbound, тихие часы UTC, readiness; настройки `INITIATIVE_*`; заготовка `initiative_queue` (Redis LIST `nf:initiative:candidates`). Тесты без БД на чистых функциях. Flutter: подпись + Tooltip к переключателю TTS. **Отложено:** индикация «идёт обработка голоса» на долгом Whisper+LLM+TTS — см. `CURRENT_STATE.md` / `TODO.md`.
- **2026-04-13:** Голос end-to-end для чата: `POST /v1/perception/tts` (OpenAI TTS); Flutter — авто-озвучка intro и текстовых ответов, переключатель «Озвучивать ответы», кнопка 🔊 у реплик ассистента; голосовой ход по-прежнему получает MP3 из `/perception/audio`. `ARCHITECTURE.md`.
- **2026-04-13:** Зафиксировано в `CURRENT_STATE.md`: «убогий» шаблонный ответ («Я слышу тебя. Ты сказал: …») — это **fallback LLM** при отсутствии ключа OpenAI или ошибке API, не целевой UX; содержательную часть диалога отлаживаем позже по плану. Flutter: реплики в ленте — `SelectableText` для копирования текста.
- **2026-04-13:** Этап 6 (первая итерация): семантическая память — Qdrant-коллекция при старте, OpenAI embeddings, индексация реплик и intro, retrieval в `generate_reply`; `SEMANTIC_MEMORY_ENABLED`, `QDRANT_*`, `EMBEDDING_*`; заготовка `memory_consolidation.py`. Документы: `ARCHITECTURE.md`, `DECISIONS.md` D-010, `ROADMAP.md` этап 6.
- **2026-04-13:** Flutter: экран создания — карточки пресетов с `genderLabelRu` и усечённой `life_legend`; модель `PersonalityPreset` парсит `gender_style` / `life_legend`.
- **2026-04-13:** Цель SRS «личность, не опросник»: пресеты дополнены `gender_style` и `life_legend`; при создании с пресетом они попадают в `IdentityCore`; системный промпт LLM — блок естественности диалога + легенда; intro без обязательного вопроса в конце. `DECISIONS.md` D-009, `PROJECT_OVERVIEW.md`.
- **2026-04-13:** Различие пресетов: в JSON — `character_prompt`, темперамент и стили; при создании передаётся `preset_id` (клиент), данные в `IdentityCore`. Память диалога: в промпт LLM добавляется недавний транскрипт активного треда (`conversation_prompt.build_recent_transcript_text`). Старые нейродрузья без `character_voice` — сбросить и создать заново с пресетом.
- **2026-04-13:** Этап 5 (часть): `GET /v1/meta/personality-presets`, `backend/app/data/personality_presets.json`, тест `test_presets.py`; Flutter — выбор пресета на экране создания; `package-data` для JSON в `pyproject.toml`.
- **2026-04-13:** Backend: `utc_naive_now()` для полей `TIMESTAMP WITHOUT TIME ZONE` (asyncpg не принимал aware datetime); модели и `affect_lite` / `chat_thread_service` обновлены.
- **2026-04-13:** Backend: при ошибке OpenAI при создании нейродруга/ответе — fallback-текст вместо 500 (`llm_orchestrator`); клиент: `formatDioError` для сообщений FastAPI.
- **2026-04-13:** `mobile/pubspec.yaml`: `record` обновлён до **^6.2.0** (исправление ошибок компиляции `record_linux` / `hasPermission` при сборке Windows); в `LOCAL_SETUP_WINDOWS.md` — режим разработчика, `flutter clean`, пояснение по симлинкам.
- **2026-04-13:** Клиент Flutter (MVP): экран дома — создание нейродруга, чат, запись WAV + TTS, инспектор debug API; `mobile/pubspec.yaml`, `mobile/README.md`, `TODO.md` / `CURRENT_STATE.md` обновлены.
- **2026-04-13:** `LOCAL_SETUP_WINDOWS.md`: `flutter run` только из `mobile` (pubspec.yaml); Android SDK cmdline-tools + `flutter doctor --android-licenses`.
- **2026-04-13:** `LOCAL_SETUP_WINDOWS.md`: для `flutter run -d windows` — Visual Studio 2022 + workload C++ desktop; альтернатива `flutter run -d chrome`.
- **2026-04-13:** `LOCAL_SETUP_WINDOWS.md`: после смены пути к проекту пересоздавать `backend\.venv` (ошибка launcher `uvicorn`/`alembic` со старым путём).
- **2026-04-13:** Flutter: добавлен `mobile/lib/core/api_config.dart`, placeholder показывает `kApiBaseUrl`; в `LOCAL_SETUP_WINDOWS.md` — предупреждение о символе `!` в пути (Windows desktop) и про вставку Dart не в PowerShell.
- **2026-04-13:** Добавлена инструкция локального запуска для Windows: `docs/LOCAL_SETUP_WINDOWS.md`; ссылка в корневом `README.md`.
- **2026-04-13:** Синхронизация с контрактом `docs/ai/*`: добавлен рабочий бэклог `TODO.md`, обновлены дата и ссылка в `CURRENT_STATE.md`; backend `pytest` — 5 passed.
- **2026-04-12:** Инициализированы `docs/ai/*`, зафиксированы обзор, архитектура (Flutter web+mобильные + FastAPI monolith), решения D-001–D-003; репозиторий кода ещё пуст.
- **2026-04-12:** Добавлен `ROADMAP.md` (этапы), решения D-004–D-006: голос OpenAI Whisper+TTS с возможностью замены, чат как не-SoT с лимитом 500/архив/10 carryover, UI отладки отношений и событий.
- **2026-04-12:** Реализован каркас backend (FastAPI, модели, Alembic, голосовой пайплайн, чат-транскрипт с rollover), debug API, placeholder mobile; pytest 5 тестов зелёные; Docker-сборка не проверена (daemon off).
- **2026-04-12:** Зафиксировано (D-008, `infra/README.md`): для dev приоритетен локальный PostgreSQL; Docker-Postgres опционален.
